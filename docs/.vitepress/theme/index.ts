// ABOUTME: Custom VitePress theme entry — extends the default theme with Armada brand accent colors.
// ABOUTME: All overrides live in custom.css; backgrounds and body text keep the default (readable) values.
import DefaultTheme from 'vitepress/theme';
import './custom.css';

export default DefaultTheme;
