// ABOUTME: Ambient module declaration for snarkjs (ships no type declarations). The prover narrows the
// ABOUTME: Groth16 surface it uses via its own interfaces; this only makes the bare import resolve.
declare module 'snarkjs';
