// ABOUTME: core/ — the PINNED, byte-compatible crypto core (keys, notes, poseidon, merkle math,
// ABOUTME: nullifiers, note ECIES, shapes, serialization). Adapted from vendor/ in Phase 1 (SPEC §2/§3.2).

export {};
