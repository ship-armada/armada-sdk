// ABOUTME: Root entry for @armada/sdk. Re-exports nothing yet — consumers import the layered
// ABOUTME: subpaths (@armada/sdk/core, /wallet, /payments, /ops). Layers land per SPEC.md phases.

export const VERSION = '0.0.0';
