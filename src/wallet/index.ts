// ABOUTME: wallet/ — enrollment, wallet lifecycle, balances, TXO set, importNote, sync orchestration,
// ABOUTME: SpendSigner custody boundary, view-only wallets. Lands in Phase 2 (SPEC §4).

export {};
